diff --git a/env.c b/env.c
--- a/env.c
+++ b/env.c
@@ -61,7 +61,7 @@
 /**
    Command used to start fishd
 */
-#define FISHD_CMD L"fishd ^/tmp/fishd.log.%s"
+#define FISHD_CMD L"@GENTOO_PORTAGE_EPREFIX@/bin/fishd ^@GENTOO_PORTAGE_EPREFIX@/var/tmp/fishd.log.%s"
 
 /**
    Value denoting a null string
diff --git a/share/config.fish.in b/share/config.fish.in
--- a/share/config.fish.in
+++ b/share/config.fish.in
@@ -72,18 +72,18 @@
 # want this even for text-only terminals.
 #
 
-set -l path_list /bin /usr/bin /usr/X11R6/bin /usr/local/bin @prefix@/bin @optbindirs@
+set -l path_list @GENTOO_PORTAGE_EPREFIX@/bin @GENTOO_PORTAGE_EPREFIX@/usr/bin @GENTOO_PORTAGE_EPREFIX@/usr/local/bin /bin /usr/bin /usr/local/bin
 
 # Root should also have the sbin directories in the path
 switch $USER
 	case root
-	set path_list $path_list /sbin /usr/sbin /usr/local/sbin @prefix@/sbin
+	set path_list $path_list @GENTOO_PORTAGE_EPREFIX@/sbin @GENTOO_PORTAGE_EPREFIX@/usr/sbin @GENTOO_PORTAGE_EPREFIX@/usr/local/sbin /sbin /usr/sbin /usr/local/sbin
 end
 
 for i in $path_list
 	if not contains $i $PATH
 		if test -d $i
-			set PATH $PATH $i
+			set PATH $i $PATH
 		end
 	end
 end
